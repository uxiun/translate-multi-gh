export const cssconst = {
  dropdown: {
    maxHeight: "max(50vh, 200px)",
    maxWidth: "80vw"
  }
}
export const propconst = {
  PaperProps: {
    sx: {
      maxHeight: cssconst.dropdown.maxHeight,
      maxWidth: cssconst.dropdown.maxWidth
    }
  }
}